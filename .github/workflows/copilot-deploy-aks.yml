name: Deploy to AKS
# This workflow deploys the application to Azure Kubernetes Service (AKS).
# It is triggered on demand or can be called from another workflow.
# It deploys the application to the specified environment (dev, test, or prod).
# The workflow consists of a single job that deploys the application to AKS.
# The job uses Azure CLI to import Docker images from Azure Container Registry (ACR) and deploys them using Helm.
# The workflow also sets up the Azure context for AKS and installs Helm.
# The workflow uses the Azure login action to authenticate with Azure and the Azure AKS set context action to set up the AKS context.
# The workflow uses the Azure setup Helm action to install Helm and the Helm upgrade action to deploy the application.

on:
  workflow_dispatch: # deploy on demand
    inputs:
      environment:
        description: "Which environment to deploy to?"
        type: environment
        required: true
        default: "dev"
  workflow_call:
    inputs:
      environment:
        description: "Which environment to deploy to?"
        type: string
        required: true
        default: "dev"

env:
  ENVIRONMENT: ${{ inputs.environment || 'dev' }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'dev' }}
    steps:
      # - name: debug variables
      #   run: |
      #     echo "environment input variable : $environmentinput"
      #     echo "environment env variable : $ENVIRONMENT"
      #     echo "RESOURCE_GROUP variable : $RESOURCE_GROUP"
      #     echo "CLUSTER_NAME variable : $CLUSTER_NAME"
      #   env:
      #     environmentinput: ${{ inputs.environment}}
      #     RESOURCE_GROUP: ${{ vars.RESOURCE_GROUP }}
      #     CLUSTER_NAME: ${{ vars.CLUSTER_NAME }}

      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'
          enable-AzPSSession: true

      - name: Tag Docker Images for Environment
        run: |
          az acr import --name ${{ vars.REGISTRY }} \
          --source ${{ vars.REGISTRY }}/chat-copilot-webapi:latest \
          -t chat-copilot-webapi:${{ inputs.environment || 'dev' }} --force
          az acr import --name ${{ vars.REGISTRY }} \
          --source ${{ vars.REGISTRY }}/chat-copilot-webapp:latest \
          -t chat-copilot-webapp:${{ inputs.environment || 'dev' }} --force

      - name: Gets K8s context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ vars.RESOURCE_GROUP }}
          cluster-name: ${{ vars.CLUSTER_NAME }}
        id: login

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Update Helm deployment
        run: |
          helm upgrade --install ${{ env.ENVIRONMENT }} \
          --namespace "copilot-${{ env.ENVIRONMENT }}" \
          --set 'podLabels.runnumber=github-${{ github.run_number }}' \
          -f helm/values.yaml \
          -f helm/values-${{ env.ENVIRONMENT }}.yaml \
          helm/
